#include <stdio.h>
#include <stdlib.h>

int main() {
    int age = 16;
    double gpa = 4.1;
    char grade = 'A'; // Note: Double quotation marks WON'T work with a single character. 
    char thisAString[] = "This is a string."; // Note: Double quotation marks WILL work with multiple characters like this.
}